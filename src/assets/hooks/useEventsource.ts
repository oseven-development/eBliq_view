/**
 * /* eslint-disable no-restricted-globals
 *
 * @format
 */

import {useState, useEffect} from 'react'
export function useEventsource(source: any) {
  const [data, setData]: any = useState([])

  return data
}
